//    31. No Users: Add an if test to Exercise 28 to make sure the list of users is not empty.
//          Sir , I think you are talking about the list of exercise: 30 here,
//                  Because there is no list in exercise: 28 
var users = ["Adil", "Atif", "Fahad", "Bilal", "Login"];
if (users.length === 0) {
    console.log("We want to find some users");
}
if (users.length != 0) {
    users.length = 0;
}
